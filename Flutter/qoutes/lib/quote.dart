class Quote {
  String content;
  String author;

  // Named parameters
  Quote({this.content = "", this.author = ""});
}

// Named Parameters
// Quote newQuote = Quote(author: "Oscar Wilde", content: "Stop dicking around");
